name: Build Application and Make Release

on:
  push:
    tags:
      - "v*"

permissions: {}

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      # Required for Azure Trusted Signing
      id-token: write
      # Required for vcpkg binary cache
      packages: write
    with:
      build-type: Release
      environment: Release
    secrets: inherit

  create_release:
    needs: build_release
    permissions:
      contents: write
    runs-on: ubuntu-slim
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          submodules: "true"
          path: "Arsenal-source"
      - name: Download artifacts
        uses: actions/download-artifact@v8
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/Arsenal-source Arsenal-${{ env.VERSION }}
          mv Arsenal-Linux-Qt6-Portable*/Arsenal-portable.tar.gz Arsenal-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv Arsenal-Linux-aarch64-Qt6-Portable*/Arsenal-portable.tar.gz Arsenal-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv Arsenal-*.AppImage/Arsenal-*-x86_64.AppImage Arsenal-Linux-x86_64.AppImage
          mv Arsenal-*.AppImage.zsync/Arsenal-*-x86_64.AppImage.zsync Arsenal-Linux-x86_64.AppImage.zsync
          mv Arsenal-*.AppImage/Arsenal-*-aarch64.AppImage Arsenal-Linux-aarch64.AppImage
          mv Arsenal-*.AppImage.zsync/Arsenal-*-aarch64.AppImage.zsync Arsenal-Linux-aarch64.AppImage.zsync
          mv Arsenal-macOS*/Arsenal.zip Arsenal-macOS-${{ env.VERSION }}.zip
          mv Arsenal-macOS*/Arsenal.dmg Arsenal-macOS-${{ env.VERSION }}.dmg

          tar --exclude='.git' -czf Arsenal-${{ env.VERSION }}.tar.gz Arsenal-${{ env.VERSION }}

          for d in Arsenal-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="Arsenal-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv Arsenal-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in Arsenal-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="Arsenal-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv Arsenal-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: Arsenal Launcher ${{ env.VERSION }}
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            Arsenal-Linux-x86_64.AppImage
            Arsenal-Linux-x86_64.AppImage.zsync
            Arsenal-Linux-aarch64.AppImage
            Arsenal-Linux-aarch64.AppImage.zsync
            Arsenal-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            Arsenal-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
            Arsenal-Windows-MinGW-w64-${{ env.VERSION }}.zip
            Arsenal-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            Arsenal-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            Arsenal-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            Arsenal-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            Arsenal-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            Arsenal-Windows-MSVC-${{ env.VERSION }}.zip
            Arsenal-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            Arsenal-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            Arsenal-macOS-${{ env.VERSION }}.zip
            Arsenal-macOS-${{ env.VERSION }}.dmg
            Arsenal-${{ env.VERSION }}.tar.gz
