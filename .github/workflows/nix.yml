name: Nix

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - "develop"
      - "release-*"
    tags:
      - "*"
    paths:
      # File types
      - "**.cpp"
      - "**.h"
      - "**.java"
      - "**.ui"

      # Build files
      - "**.nix"
      - "nix/**"
      - "flake.lock"

      # Directories
      - "buildconfig/**"
      - "cmake/**"
      - "launcher/**"
      - "libraries/**"
      - "program_info/**"
      - "tests/**"

      # Files
      - "CMakeLists.txt"

      # Workflows
      - ".github/workflows/nix.yml"
  pull_request:
    paths:
      # File types
      - "**.cpp"
      - "**.h"
      - "**.java"
      - "**.ui"

      # Build files
      - "**.nix"
      - "nix/**"
      - "flake.lock"

      # Directories
      - "buildconfig/**"
      - "cmake/**"
      - "launcher/**"
      - "libraries/**"
      - "program_info/**"
      - "tests/**"

      # Files
      - "CMakeLists.txt"

      # Workflows
      - ".github/workflows/nix.yml"
  workflow_dispatch:

permissions: {}

env:
  DEBUG: ${{ github.ref_type != 'tag' }}

jobs:
  build:
    name: Build (${{ matrix.system }})

    permissions:
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            system: x86_64-linux

          - os: ubuntu-22.04-arm
            system: aarch64-linux

          - os: macos-26
            system: aarch64-darwin

    runs-on: ${{ matrix.os }}

    env:
      CACHIX_AUTH_TOKEN: ${{ secrets.CACHIX_AUTH_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Nix
        uses: cachix/install-nix-action@v31

        # For PRs, or pushes without a Cachix token
      - name: Setup Nix Magic Cache
        if: ${{ github.event_name == 'pull_request' || env.CACHIX_AUTH_TOKEN == '' }}
        uses: DeterminateSystems/magic-nix-cache-action@v15
        with:
          diagnostic-endpoint: ""
          use-flakehub: false

        # For in-tree builds with Cachix credentials
      - name: Setup Cachix
        if: ${{ (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && env.CACHIX_AUTH_TOKEN != '' }}
        uses: cachix/cachix-action@v17
        with:
          name: arsenal
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}

      - name: Run Flake checks
        run: |
          nix flake check --print-build-logs --show-trace

      - name: Build debug package
        if: ${{ env.DEBUG == 'true' }}
        run: |
          nix build \
            --no-link --print-build-logs --print-out-paths \
            .#arsenal-debug >> "$GITHUB_STEP_SUMMARY"

      - name: Build release package
        if: ${{ env.DEBUG == 'false' }}
        run: |
          nix build --no-link --print-out-paths .#arsenal \
            | tee -a "$GITHUB_STEP_SUMMARY" \
            | tee "$RUNNER_TEMP/nix-out-paths"

      - name: Pin release package on Cachix
        if: ${{ env.DEBUG == 'false' && env.CACHIX_AUTH_TOKEN != '' }}
        env:
          TAG: ${{ github.ref_name }}
          SYSTEM: ${{ matrix.system }}
        run: |
          xargs -a "$RUNNER_TEMP/nix-out-paths" cachix pin arsenal "$TAG"-"$SYSTEM"
